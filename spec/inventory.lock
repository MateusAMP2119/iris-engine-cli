# spec/inventory.lock -- fingerprint of the spec inventory (the source of
# truth). The traceability gate fails when the doc's SHA-256 drifts from this
# value: a spec delta without a test delta. Re-record it (IRIS_TRACE_UPDATE_LOCK=1
# go test ./internal/trace -run TestSpecLockUpdate) only alongside the test delta.
spec_path: docs/Iris Specification Inventory.md
sha256: 8f0ed2d645063c5762f25a0b38cde299cbee9ced28bbf8cba07d093c9848a339
