---
status: todo
epic: E16
created: 2026-07-12
tags:
  - iris
  - task
---

# E16.4 Installer ceremony and version gate

Epic: [[Iris Epics]], E16 Install Ceremony and Pipeline Catalog
Spec: [[Iris Specification Inventory]] (source of truth; on conflict the spec wins)

## Goal

CEREMONY CUT (user, 2026-07-12): no artwork restaging — install.sh keeps today's
download/checksum/installed lines verbatim on both branches; no chapter mark, no
staged →/✓ grammar. Spec trim rides this task: the Installer-handoff Q/A drops the
"THE CLI act staged in the update grammar" promise (handoff stays; artwork clause
gone) + lock re-record. What remains is functional: transition prompt
`Set up the engine now? (Y/n)` (default yes, /dev/tty, IRIS_FORCE honored) — the
single consent for the binary's acts. Version gate before any offer: probe the
installed binary itself — `"$dest/iris" quickstart --from-installer --json` exit 0 →
exec with the flag; else plain `quickstart --json` exit 0 → exec flagless (E15-era
binary); else no handoff AND no quickstart next-step line (an old binary is never
offered a verb it lacks). Handoff execs the absolute path with `</dev/tty`, temp
cleanup + `trap - EXIT` first; never re-tie stdout. Decline/no-tty/IRIS_FORCE → plain
next-steps (quickstart line gated on the probe; `${dest}/iris` spelled out when dest
not on PATH). README updated (ceremony description, catalog mention). `sh -n` clean;
uninstall.sh untouched.

Spec: E16.1 landed the handoff Q/A; this task trims its artwork promise (delta + lock).

## Depends on

[[E16.1 Act structure and workspace prompt]] (ships `--from-installer`). May run in
parallel with E16.2/E16.3 (disjoint files: install.sh/README vs internal/cli catalog);
merges serialize.

## Contracts (2 exempt + 3 retargeted)

| Contract | Behavior | Tier |
| --- | --- | --- |
| `S08/install-ceremony-version-gate` | Probe pair, three-way fallback, gated next-steps line. | exempt |
| `S08/quickstart-install-handoff` | Retargeted prose: two-act ceremony, `Set up the engine now? (Y/n)`, --from-installer exec. | exempt |
| `S08/quickstart-decline-clean-abort` | Retargeted: decline points now include the workspace prompt / act gate. | integration |
| `S08/quickstart-adaptive-skip-running-engine` | Retargeted: skip announced under the ENGINE chapter, post-workspace-prompt. | integration |
| `S08/quickstart-full-tour` | Retargeted conformance: fresh dir IS the workspace; ceremony end state (engine running, cheat-sheet). | conformance |

## Done when

- [ ] Failing tests first for the retargeted integration/conformance rows (exempt rows: no test)
- [ ] Implemented to green; commits name the satisfied contract ids
- [ ] install.sh `sh -n` clean; non-TTY output byte-identical to pre-E16
- [ ] Traceability gate passes with no other contract broken
